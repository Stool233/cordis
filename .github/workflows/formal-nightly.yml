name: Paper trace baseline expanded models

on:
  workflow_dispatch:

jobs:
  conformance:
    runs-on: ubuntu-latest
    timeout-minutes: 155

    steps:
      - name: Check out
        uses: actions/checkout@v6
      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 24
      - name: Setup Java
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: 21
      - name: Enable Corepack
        run: corepack enable
      - name: Install
        run: yarn --no-immutable
      - name: Run expanded paper models
        run: yarn formal:model --profile nightly --quiet
      - name: Reproduce unmodified implementation mismatches
        run: yarn formal:baseline --quiet
      - name: Upload conformance evidence
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: cordis-paper-conformance-nightly
          path: formal/output/
          if-no-files-found: error
